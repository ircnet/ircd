# @(#)$Id: RELEASE_NOTES,v 1.5 1997/05/21 17:03:00 kalt Exp $

This is a development version of the package,

			USE AT YOUR OWN RISK

---

2.9.3 doesn't support 2.7 protocol anymore.  Don't run 2.9.3
and 2.7 servers on the same network.

---

New features:

	* compression of server links.
	* k lines created.
	* V lines created.
	* whois reply uses ! instead of + for the +v(oice) mode.

Important configuration changes:

	* M and P lines format has changed since 2.9.2, it is important
	  to update your ircd.conf !
	* kill lines are now case sensitive.

---

2.9.3b7 introduces a change in the PASS syntax which makes
the `link compression negociation' *incompatible* with
previous betas.
